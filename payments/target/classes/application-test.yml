spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payments_test_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: chef
    password: "!@#!@#31415"
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect