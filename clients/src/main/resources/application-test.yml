application:
  security:
    jwt: 
      secret-key:
      expiration: 86400000
      refresh-token:
        expiration: 604800000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clients_test_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: chef
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect